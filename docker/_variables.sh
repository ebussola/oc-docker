#!/bin/bash

# The project name
# Use only lowercase, no hyphens, no spaces and no special chars
octoberProjectName="example"

# plugins paths that have composer to be installed
pluginPaths=(
    "plugins/october/demo"
    # "plugins/path/to/your/plugin"
)
